# Reglas del Proyecto

## Migraciones

- Kiro puede modificar archivos de modelos (`models.py`), pero **nunca** debe ejecutar `makemigrations` ni `migrate`.
- Las migraciones las genera y aplica únicamente el desarrollador (David).
